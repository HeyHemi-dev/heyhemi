---
import BaseLayout from "./BaseLayout.astro";

export interface Props {
  title: string;
  mainId: string;
}

const { title, mainId } = Astro.props;
---

<BaseLayout title={title} mainId={mainId}>
  <slot />
</BaseLayout>
