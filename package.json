{
	"name": "heyhemi",
	"type": "module",
	"version": "0.0.1",
	"scripts": {
		"dev": "astro dev",
		"build": "astro build",
		"preview": "astro build && wrangler pages dev",
		"astro": "astro",
		"deploy:preview": "astro build && wrangler pages deploy ./dist --project-name heyhemi --branch preview",
		"deploy:main": "astro build && wrangler pages deploy ./dist --project-name heyhemi --branch main",
		"cf-typegen": "wrangler types"
	},
	"dependencies": {
		"@astrojs/cloudflare": "^12.6.12",
		"astro": "^5.17.1"
	},
	"devDependencies": {
		"@tailwindcss/vite": "^4.1.18",
		"@types/node": "^25.2.0",
		"tailwindcss": "^4.1.18",
		"wrangler": "^4.62.0"
	}
}
